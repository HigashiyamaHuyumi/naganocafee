<!DOCTYPE html>
<html>
  <head>
    <title>Nagano</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg text-black">
        <div class="container">
          <div class="navbar-brand" href="/">
            <%= link_to "NaganoCafe", root_path, style: "font-size: 2rem;" %>
          </div>

          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
  
              <% if customer_signed_in? %> <!--ログインしているときの表示-->

                <!-- ログアウトリンク -->
                <li class="nav-item">
                  <% if current_customer %>
                    <%= link_to "ログアウト", destroy_customer_session_path, method: :delete, class: "btn btn-outline-dark" %>
                  <% end %>
                </li>
                
                <li class="nav-item">
                  <% if current_admin %>
                      <%= link_to "ログアウト", destroy_admin_session_path, method: :delete, class: "btn btn-outline-dark" %>
                  <% end %>
                </li>
  
              <% else %><!--ログアウトしているときの表示-->
                <!--home-->
                <li class="nav-item">
                  <%= link_to "Home" , root_path , class: "btn btn-outline-dark" %>
                </li> 
  
                <!--about-->
                <li class="nav-item">
                  <%= link_to "About" , home_about_path , class: "btn btn-outline-dark" %>
                </li>
                
                <!--sign up-->
                <li class="nav-item">
                  <% if current_customer %>
                    <%= link_to "Sign up" , new_public_customer_registration_path , class: "btn btn-outline-dark" %>
                  <% end %>
                </li>
  
                <!--log in-->
                <li class="nav-item">
                  <% if current_customer %>
                    <%= link_to "Log in" , new_public_customer_session_path , class: "btn btn-outline-dark" %>
                  <% end %>
                </li> 
                  
                <li class="nav-item">
                  <% if current_admin %>
                    <%= link_to "Log in" , new_admin_session_path , class: "btn btn-outline-dark" %>
                  <% end %>
                </li> 
  
              <% end %>
  
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <%= yield %>
  </body>
</html>
